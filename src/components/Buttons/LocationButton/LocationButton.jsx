import { Btn, LocationIcon } from './LocationButton.styled';

const LocationButton = () => {
  return (
    <>
      <Btn type="button">
        <LocationIcon />
      </Btn>
    </>
  );
};

export default LocationButton;
